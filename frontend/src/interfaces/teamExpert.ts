export interface TeamExpert {
    imageURL: string
    name: string
    jobTitle: string
    socialNetworksURLs: {
        instagramURL?: string
        facebookURL: string
        twitterURL?: string
    }
}