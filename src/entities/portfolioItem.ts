export type PortfolioItem = {
    id: number,
    title: string
    description: string
    date: string
    client: string
    category: string
    service: string
    type: string
    imageURL: string
    blurredImageURL: string
}